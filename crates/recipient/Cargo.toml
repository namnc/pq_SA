[package]
name = "recipient"
version = "0.1.0"
edition = "2021"

[dependencies]
primitives = { path = "../primitives" }
sidecar = { path = "../sidecar" }
rand.workspace = true
rusqlite.workspace = true
